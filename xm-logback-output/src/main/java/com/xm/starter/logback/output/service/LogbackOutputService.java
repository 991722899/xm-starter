package com.xm.starter.logback.output.service;

import com.xm.starter.logback.output.model.LogbackOutputDTO;

public interface LogbackOutputService {
    void output(LogbackOutputDTO logbackOutputDTO);
}
